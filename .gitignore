node_modules
dist
.env
.env.local
.vscode
.codex-backup-wrong-local
playwright-report
test-results
Downloads/
# NOTE: package-lock.json IS committed so builds are reproducible
# and `npm audit` runs against a pinned dependency tree.
